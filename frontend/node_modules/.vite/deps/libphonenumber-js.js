import { A as isPossiblePhoneNumber$2, C as createExtensionPattern, D as WHITESPACE, E as VALID_PUNCTUATION, F as isSupportedCountry$1, I as isObject, L as metadata_min_json_default, M as Metadata$1, N as getCountryCallingCode$1, O as isValidNumber$2, P as getExtPrefix$1, S as isViablePhoneNumber, T as PLUS_CHARS, _ as chooseFormatForNumber, a as isPossiblePhoneNumber$1, b as parseRFC3966, c as normalizeArguments$2, d as parseIncompletePhoneNumber, f as parsePhoneNumberCharacter, g as PhoneNumber$1, h as ParseError, i as DIGIT_PLACEHOLDER, j as checkNumberLength, k as getNumberType$2, l as parsePhoneNumberWithError$2, m as parseDigits, n as getCountries$1, o as isValidPhoneNumber$1, p as DIGITS, r as AsYouType$1, s as parsePhoneNumber$1, t as formatIncompletePhoneNumber$1, u as parse$1, v as formatNumber$1, w as getCountryByCallingCode, x as VALID_PHONE_NUMBER_WITH_EXTENSION, y as formatRFC3966 } from "./formatIncompletePhoneNumber-CcPRcnxY.js";
//#region node_modules/libphonenumber-js/min/exports/withMetadataArgument.js
function withMetadataArgument(func, _arguments) {
	var args = Array.prototype.slice.call(_arguments);
	args.push(metadata_min_json_default);
	return func.apply(this, args);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/parsePhoneNumberWithError.js
function parsePhoneNumberWithError$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	return parsePhoneNumberWithError$2(text, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/validatePhoneNumberLength.js
function _typeof$7(o) {
	"@babel/helpers - typeof";
	return _typeof$7 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$7(o);
}
function ownKeys$2(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$2(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$2(Object(t), !0).forEach(function(r) {
			_defineProperty$3(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$2(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function _defineProperty$3(e, r, t) {
	return (r = _toPropertyKey$7(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
function _toPropertyKey$7(t) {
	var i = _toPrimitive$7(t, "string");
	return "symbol" == _typeof$7(i) ? i : i + "";
}
function _toPrimitive$7(t, r) {
	if ("object" != _typeof$7(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$7(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
function validatePhoneNumberLength$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	options = _objectSpread$2(_objectSpread$2({}, options), {}, { extract: false });
	try {
		var phoneNumber = parsePhoneNumberWithError$2(text, options, metadata);
		metadata = new Metadata$1(metadata);
		metadata.selectNumberingPlan(phoneNumber.countryCallingCode);
		var result = checkNumberLength(phoneNumber.nationalNumber, phoneNumber.country, metadata);
		if (result !== "IS_POSSIBLE") return result;
	} catch (error) {
		/* istanbul ignore else */
		if (error instanceof ParseError) return error.message;
		else throw error;
	}
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/LRUCache.js
function _typeof$6(o) {
	"@babel/helpers - typeof";
	return _typeof$6 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$6(o);
}
function _defineProperties$3(e, r) {
	for (var t = 0; t < r.length; t++) {
		var o = r[t];
		o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey$6(o.key), o);
	}
}
function _createClass$3(e, r, t) {
	return r && _defineProperties$3(e.prototype, r), t && _defineProperties$3(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function _toPropertyKey$6(t) {
	var i = _toPrimitive$6(t, "string");
	return "symbol" == _typeof$6(i) ? i : i + "";
}
function _toPrimitive$6(t, r) {
	if ("object" != _typeof$6(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$6(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
function _classCallCheck$3(a, n) {
	if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
var Node = /* @__PURE__ */ _createClass$3(function Node(key, value) {
	var next = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
	var prev = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null;
	_classCallCheck$3(this, Node);
	this.key = key;
	this.value = value;
	this.next = next;
	this.prev = prev;
});
var LRUCache = /* @__PURE__ */ function() {
	function LRUCache() {
		var limit = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 10;
		_classCallCheck$3(this, LRUCache);
		this.size = 0;
		this.limit = limit;
		this.head = null;
		this.tail = null;
		this.cache = {};
	}
	return _createClass$3(LRUCache, [
		{
			key: "put",
			value: function put(key, value) {
				this.ensureLimit();
				if (!this.head) this.head = this.tail = new Node(key, value);
				else {
					var node = new Node(key, value, this.head);
					this.head.prev = node;
					this.head = node;
				}
				this.cache[key] = this.head;
				this.size++;
			}
		},
		{
			key: "get",
			value: function get(key) {
				if (this.cache[key]) {
					var value = this.cache[key].value;
					this.remove(key);
					this.put(key, value);
					return value;
				}
				console.log("Item not available in cache for key ".concat(key));
			}
		},
		{
			key: "ensureLimit",
			value: function ensureLimit() {
				if (this.size === this.limit) this.remove(this.tail.key);
			}
		},
		{
			key: "remove",
			value: function remove(key) {
				var node = this.cache[key];
				if (node.prev !== null) node.prev.next = node.next;
				else this.head = node.next;
				if (node.next !== null) node.next.prev = node.prev;
				else this.tail = node.prev;
				delete this.cache[key];
				this.size--;
			}
		},
		{
			key: "clear",
			value: function clear() {
				this.head = null;
				this.tail = null;
				this.size = 0;
				this.cache = {};
			}
		}
	]);
}();
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/RegExpCache.js
function _typeof$5(o) {
	"@babel/helpers - typeof";
	return _typeof$5 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$5(o);
}
function _classCallCheck$2(a, n) {
	if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _defineProperties$2(e, r) {
	for (var t = 0; t < r.length; t++) {
		var o = r[t];
		o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey$5(o.key), o);
	}
}
function _createClass$2(e, r, t) {
	return r && _defineProperties$2(e.prototype, r), t && _defineProperties$2(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function _toPropertyKey$5(t) {
	var i = _toPrimitive$5(t, "string");
	return "symbol" == _typeof$5(i) ? i : i + "";
}
function _toPrimitive$5(t, r) {
	if ("object" != _typeof$5(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$5(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
var RegExpCache = /* @__PURE__ */ function() {
	function RegExpCache(size) {
		_classCallCheck$2(this, RegExpCache);
		this.cache = new LRUCache(size);
	}
	return _createClass$2(RegExpCache, [{
		key: "getPatternForRegExp",
		value: function getPatternForRegExp(pattern) {
			var regExp = this.cache.get(pattern);
			if (!regExp) {
				regExp = new RegExp("^" + pattern);
				this.cache.put(pattern, regExp);
			}
			return regExp;
		}
	}]);
}();
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/util.js
/** Returns a regular expression quantifier with an upper and lower limit. */
function limit(lower, upper) {
	if (lower < 0 || upper <= 0 || upper < lower) throw new TypeError();
	return "{".concat(lower, ",").concat(upper, "}");
}
/**
* Trims away any characters after the first match of {@code pattern} in {@code candidate},
* returning the trimmed version.
*/
function trimAfterFirstMatch(regexp, string) {
	var index = string.search(regexp);
	if (index >= 0) return string.slice(0, index);
	return string;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/utf-8.js
/**
* "\p{Z}" is any kind of whitespace or invisible separator ("Separator").
* http://www.regular-expressions.info/unicode.html
* "\P{Z}" is the reverse of "\p{Z}".
* "\p{N}" is any kind of numeric character in any script ("Number").
* "\p{Nd}" is a digit zero through nine in any script except "ideographic scripts" ("Decimal_Digit_Number").
* "\p{Sc}" is a currency symbol ("Currency_Symbol").
* "\p{L}" is any kind of letter from any language ("Letter").
* "\p{Mn}" is "non-spacing mark".
*
* Javascript doesn't support Unicode Regular Expressions
* so substituting it with this explicit set of characters.
*
* https://stackoverflow.com/questions/13210194/javascript-regex-equivalent-of-a-za-z-using-pl
* https://github.com/danielberndt/babel-plugin-utf-8-regex/blob/master/src/transformer.js
*/
var _pZ = " \xA0 ᠎ - \u2028\u2029  　";
var pZ = "[".concat(_pZ, "]");
var PZ = "[^".concat(_pZ, "]");
var _pN = "0-9²³¹¼-¾٠-٩۰-۹߀-߉०-९০-৯৴-৹੦-੯૦-૯୦-୯୲-୷௦-௲౦-౯౸-౾೦-೯൦-൵๐-๙໐-໙༠-༳၀-၉႐-႙፩-፼ᛮ-ᛰ០-៩៰-៹᠐-᠙᥆-᥏᧐-᧚᪀-᪉᪐-᪙᭐-᭙᮰-᮹᱀-᱉᱐-᱙⁰⁴-⁹₀-₉⅐-ↂↅ-↉①-⒛⓪-⓿❶-➓⳽〇〡-〩〸-〺㆒-㆕㈠-㈩㉈-㉏㉑-㉟㊀-㊉㊱-㊿꘠-꘩ꛦ-ꛯ꠰-꠵꣐-꣙꤀-꤉꧐-꧙꩐-꩙꯰-꯹０-９";
var pNd = "[".concat("0-9٠-٩۰-۹߀-߉०-९০-৯੦-੯૦-૯୦-୯௦-௯౦-౯೦-೯൦-൯๐-๙໐-໙༠-༩၀-၉႐-႙០-៩᠐-᠙᥆-᥏᧐-᧙᪀-᪉᪐-᪙᭐-᭙᮰-᮹᱀-᱉᱐-᱙꘠-꘩꣐-꣙꤀-꤉꧐-꧙꩐-꩙꯰-꯹０-９", "]");
var _pL = "A-Za-zªµºÀ-ÖØ-öø-ˁˆ-ˑˠ-ˤˬˮͰ-ʹͶͷͺ-ͽΆΈ-ΊΌΎ-ΡΣ-ϵϷ-ҁҊ-ԧԱ-Ֆՙա-ևא-תװ-ײؠ-يٮٯٱ-ۓەۥۦۮۯۺ-ۼۿܐܒ-ܯݍ-ޥޱߊ-ߪߴߵߺࠀ-ࠕࠚࠤࠨࡀ-ࡘࢠࢢ-ࢬऄ-हऽॐक़-ॡॱ-ॷॹ-ॿঅ-ঌএঐও-নপ-রলশ-হঽৎড়ঢ়য়-ৡৰৱਅ-ਊਏਐਓ-ਨਪ-ਰਲਲ਼ਵਸ਼ਸਹਖ਼-ੜਫ਼ੲ-ੴઅ-ઍએ-ઑઓ-નપ-રલળવ-હઽૐૠૡଅ-ଌଏଐଓ-ନପ-ରଲଳଵ-ହଽଡ଼ଢ଼ୟ-ୡୱஃஅ-ஊஎ-ஐஒ-கஙசஜஞடணதந-பம-ஹௐఅ-ఌఎ-ఐఒ-నప-ళవ-హఽౘౙౠౡಅ-ಌಎ-ಐಒ-ನಪ-ಳವ-ಹಽೞೠೡೱೲഅ-ഌഎ-ഐഒ-ഺഽൎൠൡൺ-ൿඅ-ඖක-නඳ-රලව-ෆก-ะาำเ-ๆກຂຄງຈຊຍດ-ທນ-ຟມ-ຣລວສຫອ-ະາຳຽເ-ໄໆໜ-ໟༀཀ-ཇཉ-ཬྈ-ྌက-ဪဿၐ-ၕၚ-ၝၡၥၦၮ-ၰၵ-ႁႎႠ-ჅჇჍა-ჺჼ-ቈቊ-ቍቐ-ቖቘቚ-ቝበ-ኈኊ-ኍነ-ኰኲ-ኵኸ-ኾዀዂ-ዅወ-ዖዘ-ጐጒ-ጕጘ-ፚᎀ-ᎏᎠ-Ᏼᐁ-ᙬᙯ-ᙿᚁ-ᚚᚠ-ᛪᜀ-ᜌᜎ-ᜑᜠ-ᜱᝀ-ᝑᝠ-ᝬᝮ-ᝰក-ឳៗៜᠠ-ᡷᢀ-ᢨᢪᢰ-ᣵᤀ-ᤜᥐ-ᥭᥰ-ᥴᦀ-ᦫᧁ-ᧇᨀ-ᨖᨠ-ᩔᪧᬅ-ᬳᭅ-ᭋᮃ-ᮠᮮᮯᮺ-ᯥᰀ-ᰣᱍ-ᱏᱚ-ᱽᳩ-ᳬᳮ-ᳱᳵᳶᴀ-ᶿḀ-ἕἘ-Ἕἠ-ὅὈ-Ὅὐ-ὗὙὛὝὟ-ώᾀ-ᾴᾶ-ᾼιῂ-ῄῆ-ῌῐ-ΐῖ-Ίῠ-Ῥῲ-ῴῶ-ῼⁱⁿₐ-ₜℂℇℊ-ℓℕℙ-ℝℤΩℨK-ℭℯ-ℹℼ-ℿⅅ-ⅉⅎↃↄⰀ-Ⱞⰰ-ⱞⱠ-ⳤⳫ-ⳮⳲⳳⴀ-ⴥⴧⴭⴰ-ⵧⵯⶀ-ⶖⶠ-ⶦⶨ-ⶮⶰ-ⶶⶸ-ⶾⷀ-ⷆⷈ-ⷎⷐ-ⷖⷘ-ⷞⸯ々〆〱-〵〻〼ぁ-ゖゝ-ゟァ-ヺー-ヿㄅ-ㄭㄱ-ㆎㆠ-ㆺㇰ-ㇿ㐀-䶵一-鿌ꀀ-ꒌꓐ-ꓽꔀ-ꘌꘐ-ꘟꘪꘫꙀ-ꙮꙿ-ꚗꚠ-ꛥꜗ-ꜟꜢ-ꞈꞋ-ꞎꞐ-ꞓꞠ-Ɦꟸ-ꠁꠃ-ꠅꠇ-ꠊꠌ-ꠢꡀ-ꡳꢂ-ꢳꣲ-ꣷꣻꤊ-ꤥꤰ-ꥆꥠ-ꥼꦄ-ꦲꧏꨀ-ꨨꩀ-ꩂꩄ-ꩋꩠ-ꩶꩺꪀ-ꪯꪱꪵꪶꪹ-ꪽꫀꫂꫛ-ꫝꫠ-ꫪꫲ-ꫴꬁ-ꬆꬉ-ꬎꬑ-ꬖꬠ-ꬦꬨ-ꬮꯀ-ꯢ가-힣ힰ-ퟆퟋ-ퟻ豈-舘並-龎ﬀ-ﬆﬓ-ﬗיִײַ-ﬨשׁ-זּטּ-לּמּנּסּףּפּצּ-ﮱﯓ-ﴽﵐ-ﶏﶒ-ﷇﷰ-ﷻﹰ-ﹴﹶ-ﻼＡ-Ｚａ-ｚｦ-ﾾￂ-ￇￊ-ￏￒ-ￗￚ-ￜ";
var pL = "[".concat(_pL, "]");
var pL_regexp = new RegExp(pL);
var pSc = "[".concat("$¢-¥֏؋৲৳৻૱௹฿៛₠-₹꠸﷼﹩＄￠￡￥￦", "]");
var pSc_regexp = new RegExp(pSc);
var pMn = "[".concat("̀-ͯ҃-֑҇-ׇֽֿׁׂׅׄؐ-ًؚ-ٰٟۖ-ۜ۟-۪ۤۧۨ-ܑۭܰ-݊ަ-ް߫-߳ࠖ-࠙ࠛ-ࠣࠥ-ࠧࠩ-࡙࠭-࡛ࣤ-ࣾऀ-ंऺ़ु-ै्॑-ॗॢॣঁ়ু-ৄ্ৢৣਁਂ਼ੁੂੇੈੋ-੍ੑੰੱੵઁં઼ુ-ૅેૈ્ૢૣଁ଼ିୁ-ୄ୍ୖୢୣஂீ்ా-ీె-ైొ-్ౕౖౢౣ಼ಿೆೌ್ೢೣു-ൄ്ൢൣ්ි-ුූัิ-ฺ็-๎ັິ-ູົຼ່-ໍཱ༹༘༙༵༷-ཾྀ-྄྆྇ྍ-ྗྙ-ྼ࿆ိ-ူဲ-့္်ွှၘၙၞ-ၠၱ-ၴႂႅႆႍႝ፝-፟ᜒ-᜔ᜲ-᜴ᝒᝓᝲᝳ឴឵ិ-ួំ៉-៓៝᠋-᠍ᢩᤠ-ᤢᤧᤨᤲ᤹-᤻ᨘᨗᩖᩘ-ᩞ᩠ᩢᩥ-ᩬᩳ-᩿᩼ᬀ-ᬃ᬴ᬶ-ᬺᬼᭂ᭫-᭳ᮀᮁᮢ-ᮥᮨᮩ᯦᮫ᯨᯩᯭᯯ-ᯱᰬ-ᰳᰶ᰷᳐-᳔᳒-᳢᳠-᳨᳭᳴᷀-ᷦ᷼-᷿⃐-⃥⃜⃡-⃰⳯-⵿⳱ⷠ-〪ⷿ-゙゚〭꙯ꙴ-꙽ꚟ꛰꛱ꠂ꠆ꠋꠥꠦ꣄꣠-꣱ꤦ-꤭ꥇ-ꥑꦀ-ꦂ꦳ꦶ-ꦹꦼꨩ-ꨮꨱꨲꨵꨶꩃꩌꪰꪲ-ꪴꪷꪸꪾ꪿꫁ꫬꫭ꫶ꯥꯨ꯭ﬞ︀-️︠-︦", "]");
var pMn_regexp = new RegExp(pMn);
var latinLetterRegexp = /* @__PURE__ */ new RegExp("[\0--ÿĀ-ſḀ-ỿƀ-ɏ̀-ͯ]");
/**
* Helper method to determine if a character is a Latin-script letter or not.
* For our purposes, combining marks should also return true since we assume
* they have been added to a preceding Latin character.
*/
function isLatinLetter(letter) {
	if (!pL_regexp.test(letter) && !pMn_regexp.test(letter)) return false;
	return latinLetterRegexp.test(letter);
}
function isInvalidPunctuationSymbol(character) {
	return character === "%" || pSc_regexp.test(character);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/matchPhoneNumberStringAgainstPhoneNumber.js
/**
* Matches a phone number object against a phone number string.
* @param  {string} phoneNumberString
* @param  {PhoneNumber} phoneNumber
* @param  {object} metadata — Metadata JSON
* @return {'INVALID_NUMBER'|'NO_MATCH'|'SHORT_NSN_MATCH'|'NSN_MATCH'|'EXACT_MATCH'}
*/
function matchPhoneNumberStringAgainstPhoneNumber(phoneNumberString, phoneNumber, metadata) {
	var phoneNumberStringContainsCallingCode = true;
	var parsedPhoneNumber = parsePhoneNumber$1(phoneNumberString, metadata);
	if (!parsedPhoneNumber) {
		phoneNumberStringContainsCallingCode = false;
		parsedPhoneNumber = parsePhoneNumber$1(phoneNumberString, { defaultCallingCode: phoneNumber.countryCallingCode }, metadata);
	}
	if (!parsedPhoneNumber) return "INVALID_NUMBER";
	if (phoneNumber.ext) {
		if (parsedPhoneNumber.ext !== phoneNumber.ext) return "NO_MATCH";
	} else if (parsedPhoneNumber.ext) return "NO_MATCH";
	if (phoneNumberStringContainsCallingCode) {
		if (phoneNumber.countryCallingCode !== parsedPhoneNumber.countryCallingCode) return "NO_MATCH";
	}
	if (phoneNumber.number === parsedPhoneNumber.number) if (phoneNumberStringContainsCallingCode) return "EXACT_MATCH";
	else return "NSN_MATCH";
	if (phoneNumber.nationalNumber.indexOf(parsedPhoneNumber.nationalNumber) === 0 || parsedPhoneNumber.nationalNumber.indexOf(phoneNumber.nationalNumber) === 0) return "SHORT_NSN_MATCH";
	return "NO_MATCH";
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/Leniency.js
/**
* Leniency when finding potential phone numbers in text segments
* The levels here are ordered in increasing strictness.
*/
var Leniency_default = {
	/**
	* Phone numbers accepted are "possible", but not necessarily "valid".
	*/
	POSSIBLE: function POSSIBLE(phoneNumber, _ref) {
		_ref.candidate;
		_ref.metadata;
		return true;
	},
	/**
	* Phone numbers accepted are "possible" and "valid".
	* Numbers written in national format must have their national-prefix
	* present if it is usually written for a number of this type.
	*/
	VALID: function VALID(phoneNumber, _ref2) {
		var candidate = _ref2.candidate;
		_ref2.defaultCountry;
		var metadata = _ref2.metadata;
		if (!phoneNumber.isValid() || !containsOnlyValidXChars(phoneNumber, candidate, metadata)) return false;
		return true;
	},
	/**
	* Phone numbers accepted are "valid" and
	* are grouped in a possible way for this locale. For example, a US number written as
	* "65 02 53 00 00" and "650253 0000" are not accepted at this leniency level, whereas
	* "650 253 0000", "650 2530000" or "6502530000" are.
	* Numbers with more than one '/' symbol in the national significant number
	* are also dropped at this level.
	*
	* Warning: This level might result in lower coverage especially for regions outside of
	* country code "+1". If you are not sure about which level to use,
	* email the discussion group libphonenumber-discuss@googlegroups.com.
	*/
	STRICT_GROUPING: function STRICT_GROUPING(phoneNumber, _ref3) {
		var candidate = _ref3.candidate, defaultCountry = _ref3.defaultCountry, metadata = _ref3.metadata, regExpCache = _ref3.regExpCache;
		if (!phoneNumber.isValid() || !containsOnlyValidXChars(phoneNumber, candidate, metadata) || containsMoreThanOneSlashInNationalNumber(phoneNumber, candidate) || !isNationalPrefixPresentIfRequired(phoneNumber, {
			defaultCountry,
			metadata
		})) return false;
		return checkNumberGroupingIsValid(phoneNumber, candidate, metadata, allNumberGroupsRemainGrouped, regExpCache);
	},
	/**
	* Phone numbers accepted are "valid" and are grouped in the same way
	* that we would have formatted it, or as a single block.
	* For example, a US number written as "650 2530000" is not accepted
	* at this leniency level, whereas "650 253 0000" or "6502530000" are.
	* Numbers with more than one '/' symbol are also dropped at this level.
	*
	* Warning: This level might result in lower coverage especially for regions outside of
	* country code "+1". If you are not sure about which level to use, email the discussion group
	* libphonenumber-discuss@googlegroups.com.
	*/
	EXACT_GROUPING: function EXACT_GROUPING(phoneNumber, _ref4) {
		var candidate = _ref4.candidate, defaultCountry = _ref4.defaultCountry, metadata = _ref4.metadata, regExpCache = _ref4.regExpCache;
		if (!phoneNumber.isValid() || !containsOnlyValidXChars(phoneNumber, candidate, metadata) || containsMoreThanOneSlashInNationalNumber(phoneNumber, candidate) || !isNationalPrefixPresentIfRequired(phoneNumber, {
			defaultCountry,
			metadata
		})) return false;
		return checkNumberGroupingIsValid(phoneNumber, candidate, metadata, allNumberGroupsAreExactlyPresent, regExpCache);
	}
};
function containsOnlyValidXChars(phoneNumber, candidate, metadata) {
	for (var index = 0; index < candidate.length - 1; index++) {
		var charAtIndex = candidate.charAt(index);
		if (charAtIndex === "x" || charAtIndex === "X") {
			var charAtNextIndex = candidate.charAt(index + 1);
			if (charAtNextIndex === "x" || charAtNextIndex === "X") {
				index++;
				if (matchPhoneNumberStringAgainstPhoneNumber(candidate.substring(index), phoneNumber, metadata) !== "NSN_MATCH") return false;
			} else {
				var ext = parseDigits(candidate.substring(index));
				if (ext) {
					if (phoneNumber.ext !== ext) return false;
				} else if (phoneNumber.ext) return false;
			}
		}
	}
	return true;
}
function isNationalPrefixPresentIfRequired(phoneNumber, _ref5) {
	_ref5.defaultCountry;
	var _metadata = _ref5.metadata;
	if (phoneNumber.__countryCallingCodeSource !== "FROM_DEFAULT_COUNTRY") return true;
	var metadata = new Metadata$1(_metadata);
	metadata.selectNumberingPlan(phoneNumber.countryCallingCode);
	phoneNumber.country || getCountryByCallingCode(phoneNumber.countryCallingCode, {
		nationalNumber: phoneNumber.nationalNumber,
		metadata
	});
	var nationalNumber = phoneNumber.nationalNumber;
	var format = chooseFormatForNumber(metadata.numberingPlan.formats(), nationalNumber);
	if (format.nationalPrefixFormattingRule()) {
		if (metadata.numberingPlan.nationalPrefixIsOptionalWhenFormattingInNationalFormat()) return true;
		if (!format.usesNationalPrefix()) return true;
		return Boolean(phoneNumber.nationalPrefix);
	}
	return true;
}
function containsMoreThanOneSlashInNationalNumber(phoneNumber, candidate) {
	var firstSlashInBodyIndex = candidate.indexOf("/");
	if (firstSlashInBodyIndex < 0) return false;
	var secondSlashInBodyIndex = candidate.indexOf("/", firstSlashInBodyIndex + 1);
	if (secondSlashInBodyIndex < 0) return false;
	if ((phoneNumber.__countryCallingCodeSource === "FROM_NUMBER_WITH_PLUS_SIGN" || phoneNumber.__countryCallingCodeSource === "FROM_NUMBER_WITHOUT_PLUS_SIGN") && parseDigits(candidate.substring(0, firstSlashInBodyIndex)) === phoneNumber.countryCallingCode) return candidate.slice(secondSlashInBodyIndex + 1).indexOf("/") >= 0;
	return true;
}
function checkNumberGroupingIsValid(number, candidate, metadata, checkGroups, regExpCache) {
	throw new Error("This part of code hasn't been ported");
}
function allNumberGroupsAreExactlyPresent(metadata, number, normalizedCandidate, formattedNumberGroups) {
	throw new Error("This part of code hasn't been ported");
}
function allNumberGroupsRemainGrouped(metadata, number, normalizedCandidate, formattedNumberGroups) {
	throw new Error("This part of code hasn't been ported");
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/parsePreCandidate.js
var SECOND_NUMBER_START_PATTERN = /[\\/] *x/;
function parsePreCandidate(candidate) {
	return trimAfterFirstMatch(SECOND_NUMBER_START_PATTERN, candidate);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/isValidPreCandidate.js
var SLASH_SEPARATED_DATES = /(?:(?:[0-3]?\d\/[01]?\d)|(?:[01]?\d\/[0-3]?\d))\/(?:[12]\d)?\d{2}/;
var TIME_STAMPS = /[12]\d{3}[-/]?[01]\d[-/]?[0-3]\d +[0-2]\d$/;
var TIME_STAMPS_SUFFIX_LEADING = /^:[0-5]\d/;
function isValidPreCandidate(candidate, offset, text) {
	if (SLASH_SEPARATED_DATES.test(candidate)) return false;
	if (TIME_STAMPS.test(candidate)) {
		var followingText = text.slice(offset + candidate.length);
		if (TIME_STAMPS_SUFFIX_LEADING.test(followingText)) return false;
	}
	return true;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findNumbers/isValidCandidate.js
var OPENING_PARENS = "(\\[（［";
var CLOSING_PARENS = ")\\]）］";
var NON_PARENS = "[^".concat(OPENING_PARENS).concat(CLOSING_PARENS, "]");
var LEAD_CLASS = "[".concat(OPENING_PARENS).concat(PLUS_CHARS, "]");
var LEAD_CLASS_LEADING = new RegExp("^" + LEAD_CLASS);
var BRACKET_PAIR_LIMIT = limit(0, 3);
/**
* Pattern to check that brackets match. Opening brackets should be closed within a phone number.
* This also checks that there is something inside the brackets. Having no brackets at all is also
* fine.
*
* An opening bracket at the beginning may not be closed, but subsequent ones should be.  It's
* also possible that the leading bracket was dropped, so we shouldn't be surprised if we see a
* closing bracket first. We limit the sets of brackets in a phone number to four.
*/
var MATCHING_BRACKETS_ENTIRE = new RegExp("^(?:[" + OPENING_PARENS + "])?(?:" + NON_PARENS + "+[" + CLOSING_PARENS + "])?" + NON_PARENS + "+(?:[" + OPENING_PARENS + "]" + NON_PARENS + "+[" + CLOSING_PARENS + "])" + BRACKET_PAIR_LIMIT + NON_PARENS + "*$");
/**
* Matches strings that look like publication pages. Example:
* <pre>Computing Complete Answers to Queries in the Presence of Limited Access Patterns.
* Chen Li. VLDB J. 12(3): 211-227 (2003).</pre>
*
* The string "211-227 (2003)" is not a telephone number.
*/
var PUB_PAGES = /\d{1,5}-+\d{1,5}\s{0,4}\(\d{1,4}/;
function isValidCandidate(candidate, offset, text, leniency) {
	if (!MATCHING_BRACKETS_ENTIRE.test(candidate) || PUB_PAGES.test(candidate)) return;
	if (leniency !== "POSSIBLE") {
		if (offset > 0 && !LEAD_CLASS_LEADING.test(candidate)) {
			var previousChar = text[offset - 1];
			if (isInvalidPunctuationSymbol(previousChar) || isLatinLetter(previousChar)) return false;
		}
		var lastCharIndex = offset + candidate.length;
		if (lastCharIndex < text.length) {
			var nextChar = text[lastCharIndex];
			if (isInvalidPunctuationSymbol(nextChar) || isLatinLetter(nextChar)) return false;
		}
	}
	return true;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/PhoneNumberMatcher.js
/**
* A port of Google's `PhoneNumberMatcher.java`.
* https://github.com/googlei18n/libphonenumber/blob/master/java/libphonenumber/src/com/google/i18n/phonenumbers/PhoneNumberMatcher.java
* Date: 08.03.2018.
*/
function _typeof$4(o) {
	"@babel/helpers - typeof";
	return _typeof$4 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$4(o);
}
function _createForOfIteratorHelperLoose(r, e) {
	var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (t) return (t = t.call(r)).next.bind(t);
	if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) {
		t && (r = t);
		var o = 0;
		return function() {
			return o >= r.length ? { done: !0 } : {
				done: !1,
				value: r[o++]
			};
		};
	}
	throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
	}
}
function _arrayLikeToArray(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
function _classCallCheck$1(a, n) {
	if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _defineProperties$1(e, r) {
	for (var t = 0; t < r.length; t++) {
		var o = r[t];
		o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey$4(o.key), o);
	}
}
function _createClass$1(e, r, t) {
	return r && _defineProperties$1(e.prototype, r), t && _defineProperties$1(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function _toPropertyKey$4(t) {
	var i = _toPrimitive$4(t, "string");
	return "symbol" == _typeof$4(i) ? i : i + "";
}
function _toPrimitive$4(t, r) {
	if ("object" != _typeof$4(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$4(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
var USE_NON_GEOGRAPHIC_COUNTRY_CODE = false;
var EXTN_PATTERNS_FOR_MATCHING = createExtensionPattern("matching");
/**
* Patterns used to extract phone numbers from a larger phone-number-like pattern. These are
* ordered according to specificity. For example, white-space is last since that is frequently
* used in numbers, not just to separate two numbers. We have separate patterns since we don't
* want to break up the phone-number-like text on more than one different kind of symbol at one
* time, although symbols of the same type (e.g. space) can be safely grouped together.
*
* Note that if there is a match, we will always check any text found up to the first match as
* well.
*/
var INNER_MATCHES = [
	"/+(.*)",
	"(\\([^(]*)",
	"(?:".concat(pZ, "-|-").concat(pZ, ")").concat(pZ, "*(.+)"),
	"[‒-―－]".concat(pZ, "*(.+)"),
	"\\.+".concat(pZ, "*([^.]+)"),
	"".concat(pZ, "+(").concat(PZ, "+)")
];
var leadLimit = limit(0, 2);
var punctuationLimit = limit(0, 4);
var digitBlockLimit = 20;
var blockLimit = limit(0, digitBlockLimit);
var punctuation = "[".concat(VALID_PUNCTUATION, "]") + punctuationLimit;
var digitSequence = pNd + limit(1, digitBlockLimit);
/**
* Phone number pattern allowing optional punctuation.
* The phone number pattern used by `find()`, similar to
* VALID_PHONE_NUMBER, but with the following differences:
* <ul>
*   <li>All captures are limited in order to place an upper bound to the text matched by the
*       pattern.
* <ul>
*   <li>Leading punctuation / plus signs are limited.
*   <li>Consecutive occurrences of punctuation are limited.
*   <li>Number of digits is limited.
* </ul>
*   <li>No whitespace is allowed at the start or end.
*   <li>No alpha digits (vanity numbers such as 1-800-SIX-FLAGS) are currently supported.
* </ul>
*/
var PATTERN = "(?:" + LEAD_CLASS + punctuation + ")" + leadLimit + digitSequence + "(?:" + punctuation + digitSequence + ")" + blockLimit + "(?:" + EXTN_PATTERNS_FOR_MATCHING + ")?";
var UNWANTED_END_CHAR_PATTERN = new RegExp("[^".concat(_pN).concat(_pL, "#]+$"));
var MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER || Math.pow(2, 53) - 1;
/**
* A stateful class that finds and extracts telephone numbers from {@linkplain CharSequence text}.
* Instances can be created using the {@linkplain PhoneNumberUtil#findNumbers factory methods} in
* {@link PhoneNumberUtil}.
*
* <p>Vanity numbers (phone numbers using alphabetic digits such as <tt>1-800-SIX-FLAGS</tt> are
* not found.
*
* <p>This class is not thread-safe.
*/
var PhoneNumberMatcher$1 = /* @__PURE__ */ function() {
	/**
	* @param {string} text — the character sequence that we will search, null for no text.
	* @param {'POSSIBLE'|'VALID'|'STRICT_GROUPING'|'EXACT_GROUPING'} [options.leniency] — The leniency to use when evaluating candidate phone numbers. See `source/findNumbers/Leniency.js` for more details.
	* @param {number} [options.maxTries] — The maximum number of invalid numbers to try before giving up on the text. This is to cover degenerate cases where the text has a lot of false positives in it. Must be >= 0.
	*/
	function PhoneNumberMatcher() {
		var text = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
		var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		var metadata = arguments.length > 2 ? arguments[2] : void 0;
		_classCallCheck$1(this, PhoneNumberMatcher);
		options = {
			v2: options.v2,
			defaultCallingCode: options.defaultCallingCode,
			defaultCountry: options.defaultCountry && isSupportedCountry$1(options.defaultCountry, metadata) ? options.defaultCountry : void 0,
			leniency: options.leniency || (options.extended ? "POSSIBLE" : "VALID"),
			maxTries: options.maxTries || MAX_SAFE_INTEGER
		};
		if (!Leniency_default[options.leniency]) throw new TypeError("Unknown leniency: \"".concat(options.leniency, "\""));
		if (options.leniency !== "POSSIBLE" && options.leniency !== "VALID") throw new TypeError("Invalid `leniency`: \"".concat(options.leniency, "\". Supported values: \"POSSIBLE\", \"VALID\"."));
		if (options.maxTries < 0) throw new TypeError("`maxTries` must be `>= 0`");
		this.text = text;
		this.options = options;
		this.metadata = metadata;
		this.leniency = Leniency_default[options.leniency];
		/** The maximum number of retries after matching an invalid number. */
		this.maxTries = options.maxTries;
		this.PATTERN = new RegExp(PATTERN, "ig");
		this.INNER_MATCHES = INNER_MATCHES.map(function(pattern) {
			return new RegExp(pattern, "g");
		});
		/** The iteration tristate. */
		this.state = "NOT_READY";
		/** The next index to start searching at. Undefined in {@link State#DONE}. */
		this.searchIndex = 0;
		this.regExpCache = new RegExpCache(32);
	}
	/**
	* Attempts to find the next subsequence in the searched sequence on or after {@code searchIndex}
	* that represents a phone number. Returns the next match, null if none was found.
	*
	* @param index  the search index to start searching at
	* @return  the phone number match found, null if none can be found
	*/
	return _createClass$1(PhoneNumberMatcher, [
		{
			key: "find",
			value: function find(index) {
				this.PATTERN.lastIndex = index;
				var matches;
				while (this.maxTries > 0 && (matches = this.PATTERN.exec(this.text)) !== null) {
					var candidate = matches[0];
					var offset = matches.index;
					candidate = parsePreCandidate(candidate);
					if (isValidPreCandidate(candidate, offset, this.text)) {
						var match = this.parseAndVerify(candidate, offset, this.text) || this.extractInnerMatch(candidate, offset, this.text);
						if (match) if (this.options.v2) return {
							startsAt: match.startsAt,
							endsAt: match.endsAt,
							number: match.phoneNumber
						};
						else {
							var phoneNumber = match.phoneNumber;
							var result = {
								startsAt: match.startsAt,
								endsAt: match.endsAt,
								phone: phoneNumber.nationalNumber
							};
							if (phoneNumber.country)
 /* istanbul ignore if */
							if (USE_NON_GEOGRAPHIC_COUNTRY_CODE && country === "001") result.countryCallingCode = phoneNumber.countryCallingCode;
							else result.country = phoneNumber.country;
							else result.countryCallingCode = phoneNumber.countryCallingCode;
							if (phoneNumber.ext) result.ext = phoneNumber.ext;
							return result;
						}
					}
					this.maxTries--;
				}
			}
		},
		{
			key: "extractInnerMatch",
			value: function extractInnerMatch(substring, offset, text) {
				for (var _iterator = _createForOfIteratorHelperLoose(this.INNER_MATCHES), _step; !(_step = _iterator()).done;) {
					var innerMatchRegExp = _step.value;
					innerMatchRegExp.lastIndex = 0;
					var isFirstMatch = true;
					var candidateMatch = void 0;
					while (this.maxTries > 0 && (candidateMatch = innerMatchRegExp.exec(substring)) !== null) {
						if (isFirstMatch) {
							var _candidate = trimAfterFirstMatch(UNWANTED_END_CHAR_PATTERN, substring.slice(0, candidateMatch.index));
							var _match = this.parseAndVerify(_candidate, offset, text);
							if (_match) return _match;
							this.maxTries--;
							isFirstMatch = false;
						}
						var candidate = trimAfterFirstMatch(UNWANTED_END_CHAR_PATTERN, candidateMatch[1]);
						var candidateIndexGuess = substring.indexOf(candidate, candidateMatch.index);
						var match = this.parseAndVerify(candidate, offset + candidateIndexGuess, text);
						if (match) return match;
						this.maxTries--;
					}
				}
			}
		},
		{
			key: "parseAndVerify",
			value: function parseAndVerify(candidate, offset, text) {
				if (!isValidCandidate(candidate, offset, text, this.options.leniency)) return;
				var phoneNumber = parsePhoneNumber$1(candidate, {
					extended: true,
					defaultCountry: this.options.defaultCountry,
					defaultCallingCode: this.options.defaultCallingCode
				}, this.metadata);
				if (!phoneNumber) return;
				if (!phoneNumber.isPossible()) return;
				if (this.leniency(phoneNumber, {
					candidate,
					defaultCountry: this.options.defaultCountry,
					metadata: this.metadata,
					regExpCache: this.regExpCache
				})) return {
					startsAt: offset,
					endsAt: offset + candidate.length,
					phoneNumber
				};
			}
		},
		{
			key: "hasNext",
			value: function hasNext() {
				if (this.state === "NOT_READY") {
					this.lastMatch = this.find(this.searchIndex);
					if (this.lastMatch) {
						this.searchIndex = this.lastMatch.endsAt;
						this.state = "READY";
					} else this.state = "DONE";
				}
				return this.state === "READY";
			}
		},
		{
			key: "next",
			value: function next() {
				if (!this.hasNext()) throw new Error("No next element");
				var result = this.lastMatch;
				this.lastMatch = null;
				this.state = "NOT_READY";
				return result;
			}
		}
	]);
}();
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/findNumbers.js
function findNumbers$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	var matcher = new PhoneNumberMatcher$1(text, options, metadata);
	var results = [];
	while (matcher.hasNext()) results.push(matcher.next());
	return results;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/searchNumbers.js
function _typeof$3(o) {
	"@babel/helpers - typeof";
	return _typeof$3 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$3(o);
}
function _defineProperty$2(e, r, t) {
	return (r = _toPropertyKey$3(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
function _toPropertyKey$3(t) {
	var i = _toPrimitive$3(t, "string");
	return "symbol" == _typeof$3(i) ? i : i + "";
}
function _toPrimitive$3(t, r) {
	if ("object" != _typeof$3(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$3(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
/**
* @return ES6 `for ... of` iterator.
*/
function searchNumbers$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	var matcher = new PhoneNumberMatcher$1(text, options, metadata);
	return _defineProperty$2({}, Symbol.iterator, function() {
		return { next: function next() {
			if (matcher.hasNext()) return {
				done: false,
				value: matcher.next()
			};
			return { done: true };
		} };
	});
}
//#endregion
//#region node_modules/libphonenumber-js/es6/findPhoneNumbersInText.js
function _typeof$2(o) {
	"@babel/helpers - typeof";
	return _typeof$2 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$2(o);
}
function ownKeys$1(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$1(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$1(Object(t), !0).forEach(function(r) {
			_defineProperty$1(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$1(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function _defineProperty$1(e, r, t) {
	return (r = _toPropertyKey$2(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
function _toPropertyKey$2(t) {
	var i = _toPrimitive$2(t, "string");
	return "symbol" == _typeof$2(i) ? i : i + "";
}
function _toPrimitive$2(t, r) {
	if ("object" != _typeof$2(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$2(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
function findPhoneNumbersInText$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	var matcher = new PhoneNumberMatcher$1(text, _objectSpread$1(_objectSpread$1({}, options), {}, { v2: true }), metadata);
	var results = [];
	while (matcher.hasNext()) results.push(matcher.next());
	return results;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/searchPhoneNumbersInText.js
function _typeof$1(o) {
	"@babel/helpers - typeof";
	return _typeof$1 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof$1(o);
}
function ownKeys(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function _defineProperty(e, r, t) {
	return (r = _toPropertyKey$1(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
function _toPropertyKey$1(t) {
	var i = _toPrimitive$1(t, "string");
	return "symbol" == _typeof$1(i) ? i : i + "";
}
function _toPrimitive$1(t, r) {
	if ("object" != _typeof$1(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof$1(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
function searchPhoneNumbersInText$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	var matcher = new PhoneNumberMatcher$1(text, _objectSpread(_objectSpread({}, options), {}, { v2: true }), metadata);
	return _defineProperty({}, Symbol.iterator, function() {
		return { next: function next() {
			if (matcher.hasNext()) return {
				done: false,
				value: matcher.next()
			};
			return { done: true };
		} };
	});
}
//#endregion
//#region node_modules/libphonenumber-js/es6/getExampleNumber.js
function getExampleNumber$1(country, examples, metadata) {
	if (examples[country]) return new PhoneNumber$1(country, examples[country], metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/parsePhoneNumberWithError.js
function parsePhoneNumberWithError() {
	return withMetadataArgument(parsePhoneNumberWithError$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/parsePhoneNumber.js
function parsePhoneNumber() {
	return withMetadataArgument(parsePhoneNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/isValidPhoneNumber.js
function isValidPhoneNumber() {
	return withMetadataArgument(isValidPhoneNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/isPossiblePhoneNumber.js
function isPossiblePhoneNumber() {
	return withMetadataArgument(isPossiblePhoneNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/validatePhoneNumberLength.js
function validatePhoneNumberLength() {
	return withMetadataArgument(validatePhoneNumberLength$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/findNumbers.js
function findNumbers() {
	return withMetadataArgument(findNumbers$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/searchNumbers.js
function searchNumbers() {
	return withMetadataArgument(searchNumbers$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/findPhoneNumbersInText.js
function findPhoneNumbersInText() {
	return withMetadataArgument(findPhoneNumbersInText$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/searchPhoneNumbersInText.js
function searchPhoneNumbersInText() {
	return withMetadataArgument(searchPhoneNumbersInText$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/PhoneNumberMatcher.js
function PhoneNumberMatcher(text, options) {
	return PhoneNumberMatcher$1.call(this, text, options, metadata_min_json_default);
}
PhoneNumberMatcher.prototype = Object.create(PhoneNumberMatcher$1.prototype, {});
PhoneNumberMatcher.prototype.constructor = PhoneNumberMatcher;
//#endregion
//#region node_modules/libphonenumber-js/min/exports/AsYouType.js
function AsYouType(country) {
	return AsYouType$1.call(this, country, metadata_min_json_default);
}
AsYouType.prototype = Object.create(AsYouType$1.prototype, {});
AsYouType.prototype.constructor = AsYouType;
//#endregion
//#region node_modules/libphonenumber-js/min/exports/isSupportedCountry.js
function isSupportedCountry() {
	return withMetadataArgument(isSupportedCountry$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/getCountries.js
function getCountries() {
	return withMetadataArgument(getCountries$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/getCountryCallingCode.js
function getCountryCallingCode() {
	return withMetadataArgument(getCountryCallingCode$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/getExtPrefix.js
function getExtPrefix() {
	return withMetadataArgument(getExtPrefix$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/Metadata.js
function Metadata() {
	return Metadata$1.call(this, metadata_min_json_default);
}
Metadata.prototype = Object.create(Metadata$1.prototype, {});
Metadata.prototype.constructor = Metadata;
//#endregion
//#region node_modules/libphonenumber-js/min/exports/getExampleNumber.js
function getExampleNumber() {
	return withMetadataArgument(getExampleNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/formatIncompletePhoneNumber.js
function formatIncompletePhoneNumber() {
	return withMetadataArgument(formatIncompletePhoneNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/min/exports/PhoneNumber.js
function PhoneNumber(number) {
	return PhoneNumber$1.call(this, number, metadata_min_json_default);
}
PhoneNumber.prototype = PhoneNumber$1.prototype;
PhoneNumber.prototype.constructor = PhoneNumber;
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/parse.js
function parseNumber() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	return parse$1(text, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/parse.js
function parse() {
	return withMetadataArgument(parseNumber, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/format.js
function formatNumber() {
	var _normalizeArguments = normalizeArguments$1(arguments), input = _normalizeArguments.input, format = _normalizeArguments.format, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	return formatNumber$1(input, format, options, metadata);
}
function normalizeArguments$1(args) {
	var arg_1 = args[0];
	var arg_2 = args[1];
	var arg_3 = args[2];
	var arg_4 = args[3];
	var arg_5 = args[4];
	var input;
	var format;
	var options;
	var metadata;
	if (typeof arg_1 === "string") if (typeof arg_3 === "string") {
		format = arg_3;
		if (arg_5) {
			options = arg_4;
			metadata = arg_5;
		} else metadata = arg_4;
		input = parse$1(arg_1, {
			defaultCountry: arg_2,
			extended: true
		}, metadata);
	} else {
		if (typeof arg_2 !== "string") throw new Error("`format` argument not passed to `formatNumber(number, format)`");
		format = arg_2;
		if (arg_4) {
			options = arg_3;
			metadata = arg_4;
		} else metadata = arg_3;
		input = parse$1(arg_1, { extended: true }, metadata);
	}
	else if (isObject(arg_1)) {
		input = arg_1;
		format = arg_2;
		if (arg_4) {
			options = arg_3;
			metadata = arg_4;
		} else metadata = arg_3;
	} else throw new TypeError("A phone number must either be a string or an object of shape { phone, [country] }.");
	if (format === "International") format = "INTERNATIONAL";
	else if (format === "National") format = "NATIONAL";
	return {
		input,
		format,
		options,
		metadata
	};
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/format.js
function format() {
	return withMetadataArgument(formatNumber, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/getNumberType.js
function getNumberType$1() {
	var _normalizeArguments = normalizeArguments(arguments), input = _normalizeArguments.input, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	if (!input.phone) return;
	return getNumberType$2(input, options, metadata);
}
function normalizeArguments(args) {
	var arg_1 = args[0];
	var arg_2 = args[1];
	var arg_3 = args[2];
	var arg_4 = args[3];
	var input;
	var options = {};
	var metadata;
	if (typeof arg_1 === "string") if (!isObject(arg_2)) {
		if (arg_4) {
			options = arg_3;
			metadata = arg_4;
		} else metadata = arg_3;
		if (isViablePhoneNumber(arg_1)) input = parse$1(arg_1, { defaultCountry: arg_2 }, metadata);
		else input = {};
	} else {
		if (arg_3) {
			options = arg_2;
			metadata = arg_3;
		} else metadata = arg_2;
		if (isViablePhoneNumber(arg_1)) input = parse$1(arg_1, void 0, metadata);
		else input = {};
	}
	else if (isObject(arg_1)) {
		input = arg_1;
		if (arg_3) {
			options = arg_2;
			metadata = arg_3;
		} else metadata = arg_2;
	} else throw new TypeError("A phone number must either be a string or an object of shape { phone, [country] }.");
	return {
		input,
		options,
		metadata
	};
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/getNumberType.js
function getNumberType() {
	return withMetadataArgument(getNumberType$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/isPossibleNumber.js
/**
* Checks if a given phone number is possible.
* Which means it only checks phone number length
* and doesn't test any regular expressions.
*
* Examples:
*
* ```js
* isPossibleNumber('+78005553535', metadata)
* isPossibleNumber('8005553535', 'RU', metadata)
* isPossibleNumber('88005553535', 'RU', metadata)
* isPossibleNumber({ phone: '8005553535', country: 'RU' }, metadata)
* ```
*/
function isPossibleNumber$1() {
	var _normalizeArguments = normalizeArguments(arguments), input = _normalizeArguments.input, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	if (!input.phone && !(options && options.v2)) return false;
	return isPossiblePhoneNumber$2(input, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/isPossibleNumber.js
function isPossibleNumber() {
	return withMetadataArgument(isPossibleNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/isValidNumber.js
function isValidNumber$1() {
	var _normalizeArguments = normalizeArguments(arguments), input = _normalizeArguments.input, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	if (!input.phone) return false;
	return isValidNumber$2(input, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/isValidNumber.js
function isValidNumber() {
	return withMetadataArgument(isValidNumber$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/isValidNumberForRegion_.js
/**
* Checks if a given phone number is valid within a given region.
* Is just an alias for `phoneNumber.isValid() && phoneNumber.country === country`.
* https://github.com/googlei18n/libphonenumber/blob/master/FAQ.md#when-should-i-use-isvalidnumberforregion
*/
function isValidNumberForRegion$2(input, country, options, metadata) {
	options = options || {};
	return input.country === country && isValidNumber$2(input, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/isValidNumberForRegion.js
function isValidNumberForRegion$1(number, country, metadata) {
	if (typeof number !== "string") throw new TypeError("number must be a string");
	if (typeof country !== "string") throw new TypeError("country must be a string");
	var input;
	if (isViablePhoneNumber(number)) input = parse$1(number, { defaultCountry: country }, metadata);
	else input = {};
	return isValidNumberForRegion$2(input, country, void 0, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/isValidNumberForRegion.js
function isValidNumberForRegion() {
	return withMetadataArgument(isValidNumberForRegion$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/PhoneNumberSearch.js
function _typeof(o) {
	"@babel/helpers - typeof";
	return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof(o);
}
function _classCallCheck(a, n) {
	if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _defineProperties(e, r) {
	for (var t = 0; t < r.length; t++) {
		var o = r[t];
		o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o);
	}
}
function _createClass(e, r, t) {
	return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function _toPropertyKey(t) {
	var i = _toPrimitive(t, "string");
	return "symbol" == _typeof(i) ? i : i + "";
}
function _toPrimitive(t, r) {
	if ("object" != _typeof(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
var WHITESPACE_IN_THE_BEGINNING_PATTERN = new RegExp("^[" + WHITESPACE + "]+");
var PUNCTUATION_IN_THE_END_PATTERN = new RegExp("[" + VALID_PUNCTUATION + "]+$");
/**
* Extracts a parseable phone number including any opening brackets, etc.
* @param  {string} text - Input.
* @return {object} `{ ?number, ?startsAt, ?endsAt }`.
*/
var PhoneNumberSearch$1 = /* @__PURE__ */ function() {
	function PhoneNumberSearch(text, options, metadata) {
		_classCallCheck(this, PhoneNumberSearch);
		this.text = text;
		this.options = options || {};
		this.metadata = metadata;
		this.state = "NOT_READY";
		this.regexp = new RegExp(VALID_PHONE_NUMBER_WITH_EXTENSION, "ig");
	}
	return _createClass(PhoneNumberSearch, [
		{
			key: "find",
			value: function find() {
				var matches = this.regexp.exec(this.text);
				if (!matches) return;
				var number = matches[0];
				var startsAt = matches.index;
				number = number.replace(WHITESPACE_IN_THE_BEGINNING_PATTERN, "");
				startsAt += matches[0].length - number.length;
				number = number.replace(PUNCTUATION_IN_THE_END_PATTERN, "");
				number = parsePreCandidate(number);
				var result = this.parseCandidate(number, startsAt);
				if (result) return result;
				return this.find();
			}
		},
		{
			key: "parseCandidate",
			value: function parseCandidate(number, startsAt) {
				if (!isValidPreCandidate(number, startsAt, this.text)) return;
				if (!isValidCandidate(number, startsAt, this.text, this.options.extended ? "POSSIBLE" : "VALID")) return;
				var result = parse$1(number, this.options, this.metadata);
				if (!result.phone) return;
				result.startsAt = startsAt;
				result.endsAt = startsAt + number.length;
				return result;
			}
		},
		{
			key: "hasNext",
			value: function hasNext() {
				if (this.state === "NOT_READY") {
					this.last_match = this.find();
					if (this.last_match) this.state = "READY";
					else this.state = "DONE";
				}
				return this.state === "READY";
			}
		},
		{
			key: "next",
			value: function next() {
				if (!this.hasNext()) throw new Error("No next element");
				var result = this.last_match;
				this.last_match = null;
				this.state = "NOT_READY";
				return result;
			}
		}
	]);
}();
createExtensionPattern("parsing");
function findPhoneNumbers$2(text, options, metadata) {
	/* istanbul ignore if */
	if (options === void 0) options = {};
	var search = new PhoneNumberSearch$1(text, options, metadata);
	var phones = [];
	while (search.hasNext()) phones.push(search.next());
	return phones;
}
/**
* @return ES6 `for ... of` iterator.
*/
function searchPhoneNumbers$2(text, options, metadata) {
	/* istanbul ignore if */
	if (options === void 0) options = {};
	var search = new PhoneNumberSearch$1(text, options, metadata);
	var iterator = function iterator() {
		return { next: function next() {
			if (search.hasNext()) return {
				done: false,
				value: search.next()
			};
			return { done: true };
		} };
	};
	var iterable = {};
	iterable[Symbol.iterator] = iterator;
	return iterable;
}
//#endregion
//#region node_modules/libphonenumber-js/es6/legacy/findPhoneNumbers.js
function findPhoneNumbers$1() {
	var _normalizeArguments = normalizeArguments$2(arguments), text = _normalizeArguments.text, options = _normalizeArguments.options, metadata = _normalizeArguments.metadata;
	return findPhoneNumbers$2(text, options, metadata);
}
/**
* @return ES6 `for ... of` iterator.
*/
function searchPhoneNumbers$1() {
	var _normalizeArguments2 = normalizeArguments$2(arguments), text = _normalizeArguments2.text, options = _normalizeArguments2.options, metadata = _normalizeArguments2.metadata;
	return searchPhoneNumbers$2(text, options, metadata);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/findPhoneNumbers.js
function findPhoneNumbers() {
	return withMetadataArgument(findPhoneNumbers$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/searchPhoneNumbers.js
function searchPhoneNumbers() {
	return withMetadataArgument(searchPhoneNumbers$1, arguments);
}
//#endregion
//#region node_modules/libphonenumber-js/index.es6.exports/PhoneNumberSearch.js
function PhoneNumberSearch(text, options) {
	PhoneNumberSearch$1.call(this, text, options, metadata_min_json_default);
}
PhoneNumberSearch.prototype = Object.create(PhoneNumberSearch$1.prototype, {});
PhoneNumberSearch.prototype.constructor = PhoneNumberSearch;
//#endregion
export { AsYouType, AsYouType$1 as AsYouTypeCustom, DIGITS, DIGIT_PLACEHOLDER, Metadata, ParseError, PhoneNumber, PhoneNumberMatcher, PhoneNumberSearch, PhoneNumberSearch$1 as PhoneNumberSearchCustom, parsePhoneNumber as default, parsePhoneNumber as parsePhoneNumberFromString, findNumbers, findPhoneNumbers, findPhoneNumbers$1 as findPhoneNumbersCustom, findPhoneNumbersInText, format, format as formatNumber, formatNumber as formatCustom, formatIncompletePhoneNumber, formatRFC3966, getCountries, getCountryCallingCode, getCountryCallingCode as getPhoneCode, getCountryCallingCode$1 as getCountryCallingCodeCustom, getCountryCallingCode$1 as getPhoneCodeCustom, getExampleNumber, getExtPrefix, getNumberType, getNumberType$1 as getNumberTypeCustom, isPossibleNumber, isPossiblePhoneNumber, isSupportedCountry, isValidNumber, isValidNumber$1 as isValidNumberCustom, isValidNumberForRegion, isValidPhoneNumber, parse, parse as parseNumber, parseNumber as parseCustom, parseDigits, parseIncompletePhoneNumber, parsePhoneNumberWithError as parsePhoneNumber, parsePhoneNumberWithError, parsePhoneNumberCharacter, parseRFC3966, searchNumbers, searchPhoneNumbers, searchPhoneNumbers$1 as searchPhoneNumbersCustom, searchPhoneNumbersInText, validatePhoneNumberLength };

//# sourceMappingURL=libphonenumber-js.js.map