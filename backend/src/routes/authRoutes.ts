import { Router } from "express";

// This file is currently not used in FlashCloud. 
// Routes are configured centrally in src/routes/routes.ts
// We are keeping this file empty to clear your TypeScript errors.

const router = Router();
export default router;
